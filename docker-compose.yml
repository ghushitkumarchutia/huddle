version: '3.8'

services:
  mongo:
    image: mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db

  valkey:
    image: valkey/valkey
    ports:
      - "6379:6379"
    volumes:
      - valkey-data:/data

volumes:
  mongo-data:
  valkey-data:
